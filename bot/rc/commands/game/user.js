import { SlashCommandBuilder, EmbedBuilder } from "discord.js";

export default {
    data: new SladhCommandBuilder()
        .setName("user")
        .setDescription("View a user's information in game")
        .add
}